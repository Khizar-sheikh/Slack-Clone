[build]
  # Directory to change to before starting a build
  base = "slack-clone"

  # Build command
  command = "npm run build"

  # Publish directory (where the built app will be)
  publish = "slack-clone/dist" # Adjust this path according to the build output directory generated by Vite
